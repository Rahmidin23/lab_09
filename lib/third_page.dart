import 'package:flutter/material.dart';

class ThirdPage extends StatelessWidget {
  const ThirdPage({super.key}); // Используем super.key

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Третья страница'));
  }
}
